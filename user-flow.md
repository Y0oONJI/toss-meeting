# User Flow

```mermaid
flowchart TD
    A([시작]) --> B[역할 선택 화면]

    B --> C[조율자로 시작하기]
    B --> D[참여자로 참여하기]

    C --> C1[회의 제목 입력]
    C1 --> C2[기간 설정]
    C2 --> C3[초대 링크/코드 생성]
    C3 --> C4[링크 공유]
    C4 --> C5[대기 화면 - 참여자 응답 모으는 중]
    C5 --> C6{교집합 존재?}

    C6 -->|있음| C7[후보 시간 확인]
    C7 --> C8[조율자 최종 확정]
    C8 --> C9([확정 완료 - 전체 공유])

    C6 -->|없음| C10[대안 제시 화면 - 조율 필요 참여자 표시]
    C10 --> C11[조율 요청 발송]
    C11 --> C12[참여자 응답 대기]
    C12 --> C13{재계산}

    C13 -->|교집합 생김| C7
    C13 -->|2회 이상 실패| C14[다수결 결과 제시]
    C14 --> C8

    D --> D1[초대 코드 입력]
    D1 --> D2[이름 / 직급 / 직군 입력]
    D2 --> D3[참석 유형 선택 - 꼭 참석 / 가능하면 참석]
    D3 --> D4[가능한 시간대 입력]
    D4 --> D5[익명 현황 화면 - 다른 참여자 시간대 확인]
    D5 --> D6{조율 요청 수신?}

    D6 -->|아니오| D7([확정 알림 수신])
    D6 -->|예| D8[2개 시간대 제안 받기]
    D8 --> D9[선택 또는 직접 입력]
    D9 --> D5

    C9 --> D7
```

---

## 다이어그램에 포함하지 않은 예외 분기

다이어그램은 happy path와 핵심 분기(교집합 유무, 조율 요청 수신 여부)만 담는다. 그 외 예외 처리는 화면 단위 디테일이라 [feature-spec.md](feature-spec.md)에서 관리한다.

- 잘못된/만료된 초대 코드 입력 (→ 9번 화면)
- 이미 확정된 회의의 코드로 참여 시도 (→ 9번 화면, 확정 알림으로 바로 이동)
- 전원 미응답 상태 또는 마감 시각 도달 시 결과 확인 (→ 3, 4번 화면)
- 조율 요청에 끝까지 무응답 → 계산에서 제외 (→ 6, 13번 화면)
- 다수결 동점 → 이른 시간대 기본 표시, 조율자 최종 선택 (→ 7번 화면)
- 확정 전 시간대 재입력 (→ 11번 화면)
