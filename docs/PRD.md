# PRD - 6명 회의 일정 조율 서비스

---

## 제품 목표

여러 사람이 회의 일정을 잡을 때 발생하는 수집 → 교집합 계산 → 재조율 루프를 시스템이 자동화하여, 조율자와 참여자 모두의 부담을 줄인다.

---

## 사용자

| 구분 | 설명 |
|------|------|
| 주요 사용자 | 조율자 — 회의를 소집하고 일정을 확정하는 사람 |
| 세컨더리 사용자 | 참여자 — 초대를 받고 가능한 시간을 입력하는 사람 |

---

## 역할 정의

| 역할 | 설명 |
|------|------|
| 조율자 | 회의를 생성하고 참여자를 초대. 어드민 역할. 최종 확정 권한 보유 |
| 꼭 참석 | 참여자가 본인이 직접 선택. 교집합 계산의 기준 |
| 가능하면 참석 | 참여자가 본인이 직접 선택. 꼭 참석 교집합 안에서 최적화 |

> 참여자에게는 조율자 외 다른 참여자가 익명으로 보임

---

## 핵심 플로우 (요약)

- **조율자**: 회의 생성 → 초대 링크 공유 → 응답 수집 → 교집합 계산 → (없으면 대안 조율) → 최종 확정 → 공유
- **참여자**: 초대 입장 → 정보/참석유형 입력 → 시간대 입력 → 익명 현황 확인 → (조율 요청 시 재입력) → 확정 확인

> 화면 단위 상세 동작은 [feature-spec.md](feature-spec.md), 분기 전체 흐름은 [user-flow.md](user-flow.md) 참고.

---

## 성공 지표 (안)

| 지표 | 의미 |
|------|------|
| 확정까지 걸린 라운드 수 | 1라운드(최초 교집합)에서 확정될수록 시스템이 조율 부담을 잘 흡수한 것 |
| 조율자 개입 횟수 | 대안 제시/조율 요청 발송 등 조율자가 수동 개입한 횟수 — 적을수록 좋음 |
| 참여자 응답률·응답 소요 시간 | 링크 공유 후 얼마나 빨리, 몇 명이 응답하는지 |
| 다수결까지 간 비율 | 전체 회의 중 2회 조율 실패로 다수결까지 간 비율 — 높으면 UX 개선 필요 신호 |

---

## 정책 결정 사항

MVP 설계 중 애매했던 지점들을 아래와 같이 결정했다.

| 항목 | 결정 | 근거 |
|------|------|------|
| 응답 마감 기한 | **있음.** 회의 생성 시 조율자가 마감 일시를 설정 | 마감이 없으면 조율자가 "언제 결과를 확인해야 하는지" 판단 기준이 없음. 알림 채널 없이도 저장된 마감 시각과 현재 시각을 비교하는 것만으로 구현 가능 (별도 알림 인프라 불필요) |
| 알림 채널 | **없음 (pull 방식).** 참여자/조율자가 같은 링크로 재접속했을 때 최신 상태를 보여주는 것으로 대체 | 로그인이 없어 참여자 연락처를 시스템이 알 수 없음. 로그인을 붙이면 채널을 가질 수 있으나 MVP 범위 밖 |
| 다수결 동점 | 후보가 여러 개면 이른 시간대를 기본으로 보여주되, 최종 확정은 조율자가 직접 선택 | 자동 판단은 최소화하고, 최종 결정권은 조율자에게 있다는 역할 정의와 일관되게 |
| 조율 요청 무응답 | 무응답자는 "불가"로 간주하지 않고 계산에서 제외. 조율자가 임의 시점에 재계산 가능 | 무응답을 불가로 자동 판단하면 참여자에게 불리한 판단이 시스템에 의해 임의로 내려짐 |
| 제출 후 재입력 | 확정 전까지 허용 (같은 링크 재접속 시 최신 값으로 덮어씀), 확정 후 잠금 | 일정 변경은 흔한 케이스이고 막을 이유가 없음 |

---

## MVP 범위 내

- 회의 생성 및 초대 링크/코드 발급
- 참여자 시간대 입력 (같은 형식)
- 시스템 교집합 자동 계산
- 교집합 없을 때 대안 제시 (조율자에게만 노출)
- 조율 실패 2회 이상 시 다수결 결과 제시
- 최종 확정 후 전체 공유
- 익명 시스템 (조율자만 어드민)

---

## MVP 범위 밖

- 캘린더 연동
- 참석 불가 옵션
- 로그인/회원가입
- 시간대 추천 기능 (로그인/히스토리 데이터 없이는 추천 기준 불명확)
